apiVersion: v1
kind: Service
metadata:
  name: mlops-server-service
  labels:
    app: mlops-server
spec:
  type: NodePort
  selector:
    app: mlops-server
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
    protocol: TCP
    name: http
